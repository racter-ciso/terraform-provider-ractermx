---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "ractermx_smtp_credential Resource - ractermx"
subcategory: ""
description: |-
  Manages a RacterMX SMTP credential.
---

# ractermx_smtp_credential (Resource)

Manages a RacterMX SMTP credential.

## Example Usage

```terraform
# Create SMTP credentials for sending email through a domain.
resource "ractermx_smtp_credential" "example" {
  domain_id   = ractermx_domain.example.id
  daily_limit = 5000
}

resource "ractermx_domain" "example" {
  name = "example.com"
}

output "smtp_username" {
  value = ractermx_smtp_credential.example.username
}

output "smtp_host" {
  value = ractermx_smtp_credential.example.smtp_config.host
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `domain_id` (Number) The numeric ID of the domain. Changing this forces a new resource.

### Optional

- `anonymous_reply_enabled` (Boolean) Whether anonymous reply is enabled. Changing this forces a new resource.
- `daily_limit` (Number) Daily send limit (1-100000, default 1000). Changing this forces a new resource.
- `proxy_domain_id` (Number) The proxy domain ID for anonymous replies. Changing this forces a new resource.

### Read-Only

- `id` (Number) The numeric ID of the SMTP credential.
- `password` (String, Sensitive) The SMTP password. Only available on creation; will be empty after import.
- `smtp_config` (Attributes) SMTP server configuration. (see [below for nested schema](#nestedatt--smtp_config))
- `username` (String) The SMTP username.

<a id="nestedatt--smtp_config"></a>
### Nested Schema for `smtp_config`

Read-Only:

- `encryption` (String) SMTP encryption type.
- `host` (String) SMTP server hostname.
- `port` (Number) SMTP server port.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Import an SMTP credential using its numeric ID.
# Note: the password will not be available after import.
terraform import ractermx_smtp_credential.example 123
```
