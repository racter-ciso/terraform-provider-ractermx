---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "ractermx_webhook Resource - ractermx"
subcategory: ""
description: |-
  Manages a RacterMX webhook endpoint.
---

# ractermx_webhook (Resource)

Manages a RacterMX webhook endpoint.

## Example Usage

```terraform
# Create a webhook endpoint to receive email event notifications.
resource "ractermx_webhook" "events" {
  url    = "https://hooks.example.com/ractermx"
  events = ["delivered", "bounced", "failed"]

  custom_headers = {
    "X-Webhook-Source" = "ractermx"
  }

  timeout_seconds = 15
  batch_enabled   = true
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `events` (List of String) List of event types to subscribe to (e.g., sent, delivered, bounced, failed, unsubscribed).
- `url` (String) The URL to receive webhook events.

### Optional

- `batch_enabled` (Boolean) Whether batch delivery is enabled.
- `custom_headers` (Map of String) Custom headers to include in webhook requests.
- `enabled` (Boolean) Whether the webhook is enabled.
- `timeout_seconds` (Number) Request timeout in seconds (5-30, default 10).

### Read-Only

- `created_at` (String) The timestamp when the webhook was created.
- `id` (Number) The numeric ID of the webhook.
- `secret` (String, Sensitive) The webhook signing secret. Only available on creation; will be empty after import.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Import a webhook using its numeric ID.
# Note: the signing secret will not be available after import.
terraform import ractermx_webhook.events 789
```
